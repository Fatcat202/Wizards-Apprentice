/// @description Target X and Y

// Inherit the parent event
event_inherited();


// Draw target_x and target_x coords for debug
if(target_x != -1 && target_y != -1) draw_sprite(spr_target, 0, target_x, target_y)

