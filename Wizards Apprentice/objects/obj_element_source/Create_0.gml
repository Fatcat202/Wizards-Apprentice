

// Pass through in creation code
	// Type of element to be spawned
//	element = None
	// Frequency at which element is spawned, in seconds
//	frequency = 1;
	// Declares if oil is to be flaming
//	is_flaming = false;


spawn_timer = 0;
spawn_length = game_get_speed(gamespeed_fps) * frequency;
